{
    "name": "gui/phpadmin",
    "description": "Gerenciador de conexoes de banco de dados (MySQL, PostgreSQL, SQLite, SQL Server) via web.",
    "type": "project",
    "license": "GPL-3.0-only",
    "require": {
        "php": ">=8.1",
        "ext-pdo": "*",
        "ext-pdo_mysql": "*",
        "ext-pdo_pgsql": "*",
        "ext-pdo_sqlite": "*"
    },
    "suggest": {
        "ext-pdo_sqlsrv": "Necessario para conectar em SQL Server (driver proprietario da Microsoft; nao disponivel via Nixpacks/nixpkgs, requer imagem Docker propria)"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        },
        "files": [
            "src/Support/helpers.php"
        ]
    }
}
