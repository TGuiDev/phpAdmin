services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3010:80"
    volumes:
      - sqlite-data:/var/www/html/storage/sqlite
    restart: unless-stopped

volumes:
  sqlite-data:
