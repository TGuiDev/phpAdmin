<!doctype html>
<html lang="pt-BR">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title><?= e($title ?? 'DB Manager') ?></title>
<link rel="stylesheet" href="/assets/css/app.css">
</head>
<body>
<header class="topbar">
    <a class="brand" href="/">DB Manager</a>
    <?php if (isset($profile)): ?>
        <nav class="breadcrumb">
            <span class="conn-label"><?= e($profile->label) ?></span>
            <?php if (isset($database)): ?>
                <span class="sep">/</span>
                <a href="/connections/<?= e($profile->id) ?>/databases/<?= rawurlencode($database) ?>/tables"><?= e($database) ?></a>
            <?php endif; ?>
            <?php if (isset($table)): ?>
                <span class="sep">/</span>
                <span><?= e($table) ?></span>
            <?php endif; ?>
        </nav>
    <?php endif; ?>
</header>

<main class="container">
    <?php foreach (\App\Support\Flash::pull() as $message): ?>
        <div class="alert alert-<?= e($message['type']) ?>"><?= e($message['message']) ?></div>
    <?php endforeach; ?>

    <?= $content ?>
</main>
</body>
</html>
